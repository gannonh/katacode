---
type: Spec
title: "2026-06-20 upstream sync closure"
description: "Closure plan for the first episodic upstream sync: the bulk merge of 80 pingdotgg/t3code commits into Kata Code on branch upstream-sync-2026-06-20, plus the sync-scoped follow-up work (branding re-application, Clerk build-injection verification, OKF integration of absorbed Effect conventions, classifier rule gaps) that must land on the integration branch before it merges to main."
tags: [fork, upstream-sync, closure, spec]
timestamp: 2026-06-20T00:00:00Z
status: Approved
approved_at: 2026-06-20T00:00:00Z
---

# 2026-06-20 upstream sync closure

## Goal

Land the first episodic upstream sync for Kata Code: absorb 80 commits from `pingdotgg/t3code` (baseline `708d5383` → upstream tip `97e5cd3bf`) as a single bulk merge on the integration branch `upstream-sync-2026-06-20`, then complete the sync-scoped follow-up work the merge causes, then land on `main`. The merge and its closure work are one accepted unit: the branch does not merge to `main` until both pass.

**Policy source of truth:** [ADR 0003 — episodic upstream sync](/adrs/0003-episodic-upstream-sync.md). **Runbook:** [upstream-sync skill](../../.agents/skills/upstream-sync/SKILL.md) and [upstream-sync guide](/guides/upstream-sync.md). **Fork baseline & divergence log:** [FORK.md](../../FORK.md).

## Current state

- Merge-base of `main` and `upstream/main` is `708d5383a` (matches the FORK.md baseline). Fork has diverged since baseline; upstream has advanced (tip may be ahead of the `97e5cd3bf` range this spec was drafted against — re-run classification after `git fetch upstream`).
- Draft classification (`sync-plan.md`, regenerated by `.agents/skills/upstream-sync/scripts/classify-upstream.ts`): **63 take / 2 reject / 11 defer / 4 review** (with `review` bucket aligned end-to-end in `rules.ts`).
- Predicted conflict surface (`conflict-zones.ts`): 649 intersecting paths, 450 in high-blast zones — heaviest in `apps/server`, `apps/web`, `apps/desktop`.
- Pre-merge divergence log recorded in [FORK.md](../../FORK.md#divergence-log): 2 rejects (marketing `3bdaa6e1`, EAS non-ported portion of `9544e72d`) and 1 ported improvement (EAS preview label gate).
- **Branch:** `upstream-sync-2026-06-20` at `774da08bc` (17 commits ahead of `main`), clean working tree. **The bulk merge of upstream commits is not yet committed.** Pre-merge deliverables below are landed on the branch; Phases 1–3 of this spec (merge, vendored repos, verify gates on the merged tree) remain open.

### Branch progress (pre-merge, landed on integration branch)

| Area                                       | Status               | Notes                                                                                                                                                                                                                                                                                                                                           |
| ------------------------------------------ | -------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Upstream-sync skill + helpers              | **Done**             | Steps 0–7 runbook; `classify-upstream.ts`, `conflict-zones.ts`, `rebrand-fork.ts`, `take-upstream.sh`                                                                                                                                                                                                                                           |
| `rebrand-fork.ts` enhancements             | **Done**             | `PROPERTY_PATTERNS` (`t3Home`, `t3-env:`, `~/.t3`); Context.Service key prefixes (`"t3/` → `"@kata-sh/code-cli/`; `"t3code-relay/` → `"@kata-sh/code-relay/`); OTel renames (`kata-server`, `kata-headless-relay-client`, `kata.client.surface`); `ElectronProtocol.ts` exempt from `"t3"` package rename (internal `t3://` scheme per FORK.md) |
| Classifier `review` vocabulary             | **Done**             | `rules.ts` emits `review` for unclassified commits; mirrored in [upstream-sync guide](/guides/upstream-sync.md)                                                                                                                                                                                                                                 |
| Hard fork: drop upstream `~/.t3` migration | **Done**             | Removed `warnLegacyHomeDirectoryIfNeeded` and legacy-T3 branding constants; [ADR 0002](/adrs/0002-katacode-product-identity.md) consequences updated                                                                                                                                                                                            |
| Desktop dev launcher / auth callback       | **Done**             | `electron-launcher.mjs` consolidation, loopback Vite bind, dev auth callback server in running app; tests in `electron-launcher.test.mjs`                                                                                                                                                                                                       |
| OTel defaults in source                    | **Done**             | `packages/shared/src/relayTracing.ts`, `apps/server/src/cloud/relayTracing.ts`, `apps/server/src/observability/Layers/Observability.ts`                                                                                                                                                                                                         |
| Bulk merge (`git merge upstream/main`)     | **Not started**      | Resume sequence in [handoff](/specs/2026-06-20-upstream-sync-handoff.md)                                                                                                                                                                                                                                                                        |
| Closure Task 2 (Clerk build-injection)     | **Blocked**          | Requires merged `DesktopClerk.ts` / build defines from upstream                                                                                                                                                                                                                                                                                 |
| Closure Task 3 (OKF Effect conventions)    | **Blocked**          | Requires merged `.macroscope/check-run-agents/effect-service-conventions.md`                                                                                                                                                                                                                                                                    |
| Closure Task 4 (classifier rule gaps)      | **Done (pre-merge)** | `review` bucket fix addresses the mis-bucketed commits; no additional deterministic rules warranted for the four `review`-bucket SHAs (resolved to take in Decisions 5 and 8)                                                                                                                                                                   |
| Closure Task 5 (vendored repos)            | **Blocked**          | Runs after merge when Effect `4.0.0-beta.78` lands                                                                                                                                                                                                                                                                                              |

## Constraints

- Bulk merge only; no cherry-pick split (the 39 `[codex]` Effect service refactor commits depend on each other and break compilation if taken individually).
- All fork-policy resolution rules in the runbook's Step 3 apply: restore `Kata Code` / `KATACODE_*` / `katacode://` / `@kata-sh/code-*` / `com.katacode.app` branding on product surfaces; never reintroduce `@t3tools/*` or `T3CODE_*` without an explicit FORK.md decision; do not hand-merge `pnpm-lock.yaml` (regenerate via `vp i`); keep fork rebrand test fixtures upstream-shaped where required (`octocat/t3code`).
- Keep fork wire identifiers intact: `kata_relay`, `kata-mobile`, `kata-web`, `/.well-known/kata/environment`, `/api/kata-connect` (see FORK.md deferred wire-identity section).
- Verify gates required by AGENTS.md: `vp check` and `vp run typecheck` must pass before landing. `vp run test`, `vp run release:smoke` (if release paths touched), `vp run lint:mobile` (if native mobile touched) as applicable.
- Closure work lands on the integration branch. `main` is not touched until closure is complete.

## Out of scope

- Cross-sync deferred work (e.g. Mobile EAS production release, Connect open signups, marketing Connect pages) — these belong in `docs/specs/deferred-work.md`, not this spec.
- Changing fork sync policy (ADR 0003) or the runbook itself.
- Contributing changes back upstream.

## Accepted approach (Decisions 1-9)

The classification review produced nine decisions, all approved in conversation and recorded here as the accepted approach:

1. **Merge strategy (bulk):** `git merge upstream/main` (all 80 commits at once) on `upstream-sync-2026-06-20`. Single bulk merge, not waves or cherry-picks.
2. **`[codex]` relay commits (4):** `e95b57dc2`, `53d0107759`, `d5a72d5be1`, `b17b6d50c5`. Take with the bulk merge. Resolve `infra/relay/` conflicts manually, keeping kata wire identifiers; accept upstream's Effect structural changes.
3. **Desktop auth migration (`3e01c4bc57`, "Migrate desktop auth to Clerk bridge"):** Take with the bulk merge. Accept upstream's deletions of `DesktopCloudAuth.ts`, `DesktopCloudAuthTokenStore.ts`, `cloudAuth.ts`; take upstream's new `DesktopClerk.ts` (using `@clerk/electron` + `@clerk/electron-passkeys`) and `DesktopLocalEnvironmentAuth.ts`. Restore Kata branding on new product-facing strings; fix `@t3tools/*` imports the new files carry (pre-rename: `@t3tools/shared/relayAuth`, `@t3tools/desktop/...`); remap the `__T3CODE_BUILD_CLERK_PUBLISHABLE_KEY__` build-time constant to `__KATACODE_BUILD_CLERK_PUBLISHABLE_KEY__`. Dedicated resolution pass; highest single-merge effort.
4. **Upstream-internal docs-only commits (5):** the `.macroscope/check-run-agents/effect-service-conventions.md` changes — specifically `6b74476322`, `10a8d3fbd9`, `6d2dae0627`, `2fbbe68c24`, `3900c45f25`. Take with the bulk merge (additive, near-zero conflict). OKF synthesis is closure Task 3 below.
5. **Mobile-surface commits (2):** `ae2b523503` (react-native-shiki-engine 0.3.12 bump), `e29ad7604b` (Unify mobile typography tokens). Take with the bulk merge. Mobile is a future project phase (not indefinitely deferred); keeping `apps/mobile/` current prevents rot.
6. **EAS preview workflow (`9544e72d`):** Hand-port the label gate into `.github/disabled/mobile-eas-preview.yml` (already recorded in FORK.md); reject the non-disabled path re-add and the `blacksmith-8vcpu-ubuntu-2404` runner. Implementation happens during merge resolution.
7. **Marketing homepage (`3bdaa6e1`):** Reject (already recorded in FORK.md).
8. **Remaining review-bucket commits (4):** `804d44cfb1` (ssh fnm fix), `cdaba7fa8b` (thread state idle TTL), `ae2b523503` (mobile shiki bump — also Decision 5), `e29ad7604b` (mobile typography — also Decision 5). All take with the bulk merge. None tied to an un-integrated feature; the classifier had no rule signal (now correctly emitted as `review`, resolved to `take`).
9. **`[codex]` desktop Electron refactor (`b19fc1b87b`):** Take with the bulk merge. The classifier bucketed this as `defer` (take+defer) because it touches `apps/desktop/src/electron/ElectronProtocol.ts` — the internal `t3://` static-asset scheme divergence surface (see FORK.md). This is a take-with-manual-resolution case, sibling to Decisions 2 and 3: accept the coordinated Effect refactor, resolve the `ElectronProtocol.ts` conflict keeping the internal `t3://` scheme intact (renaming it is a separately deferred Phase 2 item, not in scope for this sync).
10. **Net classification:** all 80 commits absorbed via single bulk merge. 2 rejects, 1 ported-modified (`9544e72d`), 0 cherry-picks. Arithmetic: 63 take + 11 defer + 4 review = 78 absorbed; plus 2 rejects (marketing `3bdaa6e1`, EAS non-ported portion of `9544e72d`) = 80. Every defer and review verdict resolves to take per Decisions 2-9; the `review` bucket's 4 commits are all taken, the `defer` bucket's 11 commits are all take-with-manual-resolution.

## Implementation phases

### Phase 1 — Merge (Step 3 of the runbook)

`git merge upstream/main` on the integration branch. Resolve by zone in this order: `pnpm-lock.yaml` (regen via `vp i`), `apps/server`, `apps/web`, `apps/desktop` (Clerk migration as its own pass), `infra/relay`, `packages/contracts` / `packages/shared`, root and app `package.json`.

For `9544e72d`: discard upstream's `.github/workflows/mobile-eas-preview.yml` recreation; add the two label-gate lines to `.github/disabled/mobile-eas-preview.yml` (do not take Blacksmith runner).

### Phase 2 — Vendored repos (Step 4)

Upstream bumped Effect to `4.0.0-beta.78` (and `@effect/vitest`, `@effect/tsgo`, etc.) in `pnpm-workspace.yaml`. Run `vp run sync:repos` to sync the vendored Effect subtree under `.repos/` to the installed version. If Alchemy or another vendored dep was also bumped, sync those repositories too. Keep `.repos/` matched to installed versions. This is not conditional: Effect was bumped, so the sync runs.

### Phase 3 — Verify gates (Step 5)

`vp i` (after lockfile regen), `vp run --filter @kata-sh/code-desktop ensure:electron`, `vp check`, `vp run typecheck`, `vp run test`, `vp run release:smoke` (release paths touched), `vp run lint:mobile` (native mobile touched). Manual smoke of `pnpm run dev` and `pnpm run dev:desktop` when session UX or providers changed. Do not proceed to closure until `vp check` and `vp run typecheck` pass.

### Phase 4 — Closure tasks (Step 6)

See Closure tasks below. Each has acceptance criteria. Route via `plan-build-verify` Build/Verify phases.

### Phase 5 — Land (Step 7)

After closure passes: `git checkout main`, `git merge upstream-sync-2026-06-20`, `git push origin main`. Update FORK.md "Last upstream sync" block. OKF log entry.

## Closure tasks

### Task 1 — Branding re-application

Verify and restore Kata branding on every product surface the merge touched. The new `DesktopClerk.ts` and `DesktopLocalEnvironmentAuth.ts` files carry pre-rename `@t3tools/*` imports and the `__T3CODE_BUILD_CLERK_PUBLISHABLE_KEY__` define. Scan the merged tree for any `@t3tools/`, `T3CODE_`, `t3code://`, `app.t3.codes` regressions on product surfaces (test fixtures may legitimately remain upstream-shaped: `octocat/t3code`).

### Task 2 — Clerk publishable key build-injection verification

Confirm the `__KATACODE_BUILD_CLERK_PUBLISHABLE_KEY__` (remapped from upstream's `__T3CODE_BUILD_CLERK_PUBLISHABLE_KEY__`) is wired through the fork's build (vite define / electron build) and sourced from the GitHub `production` environment per the relay-deploy spec. A branded build must ship with the publishable key injected, not missing. Flag if the release workflow's existing Clerk config path doesn't supply this define.

### Task 3 — OKF Effect conventions synthesis

Synthesize upstream's `.macroscope/check-run-agents/effect-service-conventions.md` (absorbed in Phase 1) into a contributor-facing doc under `docs/reference/` or by extending `docs/operations/effect-fn-checklist.md`, with OKF frontmatter. Cross-link the `.macroscope` prompt as the authoritative machine-readable version and the synthesized doc as the human-readable version. Update OKF logs.

### Task 4 — Classifier rule gaps surfaced by the merge

Audit whether any merge-exposed case should have produced a deterministic Take/Reject/Defer rather than `review`, and add a rule to `scripts/rules.ts` if so. (The `review` bucket itself is now correct; this task is about reducing future `review` noise.) If no new rules are warranted, record that decision.

**Pre-merge record (2026-06-21):** The `review` bucket was added to `rules.ts` so unclassified commits no longer mis-bucket as `defer`. Re-running classification on the baseline range yields 63 take / 2 reject / 11 defer / 4 review. The four `review` commits (`804d44cfb1`, `cdaba7fa8b`, `ae2b523503`, `e29ad7604b`) were human-resolved to **take** in Decisions 5 and 8; they are edge cases without a safe deterministic rule (ssh fnm path, thread idle TTL, mobile dep bumps). **No additional rules added.** Re-audit after the bulk merge if new `review` noise appears.

### Task 5 — Vendored-repo convergence confirmation

Effect was bumped to `4.0.0-beta.78` upstream (verified), so Phase 2 will run `vp run sync:repos` to sync the vendored Effect subtree. If any repo fails to sync or versions do not match installed deps after the sync, resolve here. If it converged fully, record that with the command output.

## Acceptance criteria

### Merge and verify gates

1. The integration branch `upstream-sync-2026-06-20` contains a single merge commit absorbing all 80 upstream commits since `708d5383` (verifiable via `git log --oneline 708d5383..upstream-sync-2026-06-20` showing the upstream range and the merge).
2. `vp check` exits 0 on the merged branch (pre-commit hook is not sufficient; the full check must pass).
3. `vp run typecheck` exits 0 across all workspace packages on the merged branch.
4. `vp run test` passes. If a test cannot pass because of fork divergence, before landing the spec must record, for each such test: (a) the failing assertion, (b) the fork divergence causing it, and (c) the resolution path (a follow-up closure task, an entry in `docs/specs/deferred-work.md`, or a fork-only test stub). A bare "blocked" status is not acceptable.
5. `vp run release:smoke` passes if release paths were touched (determined during Phase 1-3).
6. `pnpm-lock.yaml` is regenerated via `vp i` (not hand-merged) and the regenerated lockfile is committed.

### Fork-policy resolution

1. A repo-wide scan of the merged branch for `@t3tools/`, `T3CODE_`, `t3code://`, and `app.t3.codes` on product surfaces returns zero matches outside (a) test fixtures (upstream-shaped `octocat/t3code`) and (b) the `t3://` literal inside `apps/desktop/src/electron/ElectronProtocol.ts` and any bundled-asset registration files it references, which remain intentionally upstream-shaped per FORK.md. The exemption is path- and literal-scoped, not a blanket `t3://` allowance.
2. Fork wire identifiers are intact in the merged `infra/relay/` source: `kata_relay`, `kata-mobile`, `kata-web`, `/.well-known/kata/environment`, `/api/kata-connect`.
3. `.github/disabled/mobile-eas-preview.yml` contains the two ported label-gate lines (`types: [opened, reopened, synchronize, labeled, unlabeled]` under `on: pull_request:`; `if: contains(github.event.pull_request.labels.*.name, '🚀 Mobile Continuous Deployment')` under the `preview:` job) and does not use `blacksmith-8vcpu-ubuntu-2404`.
4. `.github/workflows/mobile-eas-preview.yml` does not exist on the merged branch (the upstream re-add was discarded).

### Closure tasks

1. **Task 1 (branding):** The merged `DesktopClerk.ts` and `DesktopLocalEnvironmentAuth.ts` import from `@kata-sh/code-*` (not `@t3tools/*`); the build-time define is `__KATACODE_BUILD_CLERK_PUBLISHABLE_KEY__` (not `T3CODE`). A scan confirming no `@t3tools/` or `T3CODE_` regressions on product surfaces is documented with the exact command and its zero-match output.
2. **Task 2 (Clerk build-injection):** The fork's build (vite define / electron build) injects `__KATACODE_BUILD_CLERK_PUBLISHABLE_KEY__` from the GitHub `production` environment. Evidence artifact: the build config diff (or the release workflow secret wiring) showing the define is sourced, plus a build that compiles with the define resolved. If the release workflow cannot supply this define without storing a long-lived secret, Build stops, records the question and the rejected approaches to `docs/specs/log.md`, and does not land the branch until the human resolves it.
3. **Task 3 (OKF synthesis):** A new or updated contributor doc under `docs/reference/` or `docs/operations/` distills the absorbed Effect conventions with OKF frontmatter. The doc cross-links the `.macroscope/check-run-agents/effect-service-conventions.md` prompt. `docs/log.md` and the relevant section `log.md` have dated entries.
4. **Task 4 (classifier rules):** Either (a) a new rule is added to `scripts/rules.ts` with a rationale tied to a concrete commit the merge exposed, and re-running `node .agents/skills/upstream-sync/scripts/classify-upstream.ts` produces output in which the target commit is no longer in the `review` bucket (the script runs against the upstream range since baseline, not a merge state); or (b) a note in this spec records that no new rule was warranted, with the audit reasoning.
5. **Task 5 (vendored repos):** `vp run sync:repos` converges fully (all configured repos match installed dep versions), or each non-converged repo is documented with a resolution path. Evidence: command output.

### Roadmap and record

1. The `docs/specs/index.md` roadmap row for this sync is promoted from Planned to Active and links this closure spec; on landing it is marked Completed with the fork SHA.
2. The FORK.md "Last upstream sync" block is updated with the merged upstream tip SHA, the fork SHA after merge, the conflict zones resolved, the verification commands run, and a pointer to this closure spec.
3. The OKF bundle logs (`docs/log.md`, `docs/specs/log.md`) have a dated entry for the landed sync referencing this spec and the FORK.md update.

## Verification

- Gate commands: `vp check`, `vp run typecheck`, `vp run test`, `vp run release:smoke` (if applicable), `vp run lint:mobile` (if applicable).
- Branding scan commands documented with output.
- Clerk build-injection evidence: build config diff and a successful branded build.
- Classifier evidence: re-run `node .agents/skills/upstream-sync/scripts/classify-upstream.ts` output showing the new rule (if any) re-bucketing the target commit.
- Vendored-repo evidence: `vp run sync:repos` output.

## Risks and mitigations

- **Desktop auth migration is large and modify/delete.** The fork has branding commits on the files upstream deletes. Mitigation: dedicated resolution pass; accept upstream's deletions; re-apply branding on the new files; regenerate tests.
- **Clerk build-injection may require release-workflow changes beyond a define rename.** Mitigation: Task 2 is a hard gate; Build stops and asks if the define cannot be sourced without a long-lived secret.
- **`infra/relay/` heavy intersection (20+ files both sides).** Mitigation: resolve by zone, keeping kata wire identifiers; resolve in the same pass as the `[codex]` relay commits.
- **A branded desktop build may fail to ship without the publishable key.** Mitigation: Task 2 acceptance criterion requires a successful branded build with the define resolved before landing.
- **Merge may surface more branding regressions than expected (450 high-blast files).** Mitigation: Task 1 scan is a hard gate; do not land until the scan is clean outside known exemptions.

## Build handoff

**Approved scope:** Phase 1 (merge), Phase 2 (vendored repos), Phase 3 (verify gates), Phase 4 (five closure tasks), Phase 5 (land). Decisions 1-9 are the accepted classification approach; do not re-debate them during Build without an explicit user override.

**Required verification before landing:** all Verify gates pass; all five closure-task acceptance criteria pass; FORK.md "Last upstream sync" block updated; OKF logs updated; roadmap row promoted to Completed.

**Blocking questions for Build:** none at spec time. If Task 2 surfaces that the release workflow cannot source `__KATACODE_BUILD_CLERK_PUBLISHABLE_KEY__` without a long-lived secret, Build stops and asks the user.

## Explicitly deferred work

- Mobile EAS production release (Phase 2 mobile) — `docs/specs/deferred-work.md`.
- Connect open signups (waitlist off) — `docs/specs/deferred-work.md`.
- Marketing Connect and release pages — `docs/specs/deferred-work.md`.
- Renaming the internal `t3://` static asset scheme (Phase 2 deferred) — FORK.md.
